services:
  dev:
    build: .
    working_dir: /work
    volumes:
      - .:/work
      - cargo-registry:/usr/local/cargo/registry
      - cargo-git:/usr/local/cargo/git
      - target:/work/target

volumes:
  cargo-registry:
  cargo-git:
  target:
